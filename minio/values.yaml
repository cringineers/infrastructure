---
mode: standalone
auth:
  existingSecret: minio-secret

tls:
  enabled: true
  autoGenerated: true

statefulset:
  replicaCount: 2

persistence:
  enabled: true
  storageClass: "csi-ceph-hdd-ms1"
  size: 10Gi
